firecrawl>=4.20.0
python-dotenv>=1.0.0
streamlit>=1.40.0
pandas>=2.2.0
requests>=2.31.0
anthropic>=0.40.0
psycopg2-binary>=2.9.9
matplotlib>=3.8.0
# Pin httpcore + httpx eksplicit. Uden disse linjer backtrack'er pip gennem
# 80+ httpx-versioner naar PyPI er ustabilt og kan ikke faa fat i httpcore
# (transient timeout 2026-06-11) -> ResolutionImpossible. Med eksplicit pin
# henter pip httpcore med det samme uden at gaette sig frem.
httpcore==1.0.9
httpx==0.28.1
